[pytest]
testpaths = tests
markers =
    integration: calls the live Infrared API — requires INFRARED_API_KEY env var
# importmode=importlib prevents pytest from adding the rootdir to sys.path,
# which would cause the plugin's thirdparty/numpy to shadow the system numpy.
addopts = -v --tb=short --import-mode=importlib
